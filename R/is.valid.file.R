# TODO: Documentation de is.valid.file.R
# 
# Author: David GOHEL <david.gohel@lysis-consultants.fr>
# Date: 3 mai 2013
# Version: 0.1
###############################################################################

#is.valid.file = function ( file, type = c("png", "gif", "wmf"), check.exists = T ){
#	file = normalizePath(file, winslash = "/", mustWork = F)
#	if( check.exists && !file.exists( file ) ){
#		return ( F )
#	}
#	.reg = regexpr( paste( "(\\.(?i)(", paste( casefold(type, upper=F), collapse = "|"), "))$", sep = "" ), file )
#	if( .reg < 1 ) return ( F )
#	
#	return( T )	
#}

